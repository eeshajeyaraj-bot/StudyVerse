import { useEffect, useMemo, useRef, useState } from 'react'
import { supabase } from '../lib/supabase'
import { useAuth } from '../context/AuthContext'
const F='id,name,display_name,avatar_url,avatar_emoji,bio'; const nm=p=>p?.display_name||p?.name||'StudyVerse member'; const av=p=>p?.avatar_url?<img className="sv-avatar" src={p.avatar_url} alt=""/>:<span className="sv-avatar sv-avatar-emoji">{p?.avatar_emoji||nm(p).slice(0,1)}</span>
export default function Friends(){const {user}=useAuth(),uid=user?.id;const[profile,setProfile]=useState(null),[friends,setFriends]=useState([]),[requests,setRequests]=useState([]),[search,setSearch]=useState(''),[results,setResults]=useState([]),[selected,setSelected]=useState(null),[messages,setMessages]=useState([]),[text,setText]=useState(''),[editing,setEditing]=useState(null),[editText,setEditText]=useState(''),[menu,setMenu]=useState(null),[nickname,setNickname]=useState(''),[notice,setNotice]=useState(''),[loading,setLoading]=useState(true),[uploading,setUploading]=useState(false);const fileRef=useRef(),cameraRef=useRef();const accepted=useMemo(()=>new Set(friends.map(r=>r.user_id===uid?r.friend_id:r.user_id)),[friends,uid]);
 async function load(){if(!uid)return;setLoading(true);const[a,b]=await Promise.all([supabase.from('profiles').select(F).eq('id',uid).maybeSingle(),supabase.from('friendships').select('id,user_id,friend_id,status,nickname,created_at').or(`user_id.eq.${uid},friend_id.eq.${uid}`).order('created_at',{ascending:false})]);if(a.data)setProfile(a.data);if(b.error){setNotice(b.error.message);setLoading(false);return}const ids=[...new Set((b.data||[]).flatMap(r=>[r.user_id,r.friend_id]).filter(Boolean))];const p=ids.length?await supabase.from('profiles').select(F).in('id',ids):{data:[]};const map=new Map((p.data||[]).map(x=>[x.id,x]));const rows=(b.data||[]).map(r=>({...r,user:map.get(r.user_id),friend:map.get(r.friend_id)}));setFriends(rows.filter(r=>r.status==='accepted'));setRequests(rows.filter(r=>r.friend_id===uid&&r.status==='pending'));setLoading(false)}useEffect(()=>{load()},[uid]);
 useEffect(()=>{const q=search.trim().replace(/[%_]/g,'');if(q.length<2||!uid){setResults([]);return}const t=setTimeout(async()=>{const[a,b]=await Promise.all([supabase.from('profiles').select(F).neq('id',uid).ilike('display_name',`%${q}%`).limit(12),supabase.from('profiles').select(F).neq('id',uid).ilike('name',`%${q}%`).limit(12)]);if(a.error||b.error){setNotice((a.error||b.error).message);return}setResults([...new Map([...(a.data||[]),...(b.data||[])].map(x=>[x.id,x])).values()])},250);return()=>clearTimeout(t)},[search,uid]);
 async function request(id){const{error}=await supabase.from('friendships').insert({user_id:uid,friend_id:id,status:'pending'});if(error){setNotice(error.message);return}await supabase.from('notifications').insert({user_id:id,type:'friend_request',title:'New friend request',message:`${nm(profile)} sent you a friend request.`,link:'/friends',actor_id:uid,metadata:{requester_id:uid}});setSearch('');setResults([]);load()}
 async function respond(id,status){const{data:r}=await supabase.from('friendships').select('user_id').eq('id',id).eq('friend_id',uid).maybeSingle();const{error}=await supabase.from('friendships').update({status}).eq('id',id).eq('friend_id',uid);if(error){setNotice(error.message);return}if(status==='accepted'&&r?.user_id)await supabase.from('notifications').insert({user_id:r.user_id,type:'friend_request',title:'Friend request accepted',message:`${nm(profile)} accepted your request.`,link:'/friends',actor_id:uid,metadata:{friendship_id:id}});load()}
 async function open(row){const p=row.user_id===uid?row.friend:row.user;if(!p)return;setSelected({...p,friendshipId:row.id,nickname:row.nickname||''});setNickname(row.nickname||'');setMenu(null);const{data,error}=await supabase.from('direct_messages').select('*').or(`and(sender_id.eq.${uid},recipient_id.eq.${p.id}),and(sender_id.eq.${p.id},recipient_id.eq.${uid})`).order('created_at');if(error)setNotice(error.message);else setMessages(data||[])}
 useEffect(()=>{if(!selected||!uid)return;const c=supabase.channel(`dm:${uid}:${selected.id}`).on('postgres_changes',{event:'*',schema:'public',table:'direct_messages'},p=>{const m=p.new;if(!m||!((m.sender_id===uid&&m.recipient_id===selected.id)||(m.sender_id===selected.id&&m.recipient_id===uid)))return;if(p.eventType==='DELETE')setMessages(x=>x.filter(a=>a.id!==p.old.id));else setMessages(x=>[...x.filter(a=>a.id!==m.id),m].sort((a,b)=>new Date(a.created_at)-new Date(b.created_at)))}).subscribe();return()=>supabase.removeChannel(c)},[selected?.id,uid]);
 async function send(body=text,att=null){if(!selected||!uid||(!body.trim()&&!att))return;const{error}=await supabase.from('direct_messages').insert({sender_id:uid,recipient_id:selected.id,body:body.trim()||'📎 Attachment',...(att||{})});if(error){setNotice(error.message);return}await supabase.from('notifications').insert({user_id:selected.id,type:'message',title:`Message from ${nm(profile)}`,message:(body.trim()||'Sent an attachment').slice(0,180),link:'/friends',actor_id:uid,metadata:{sender_id:uid}});setText('')}
 async function upload(file){if(!file)return;setUploading(true);const ext=(file.name.split('.').pop()||'bin').toLowerCase(),path=`${uid}/dm-${Date.now()}-${Math.random().toString(36).slice(2)}.${ext}`;const u=await supabase.storage.from('studyverse-chat').upload(path,file);if(u.error){setNotice(u.error.message);setUploading(false);return}const s=await supabase.storage.from('studyverse-chat').createSignedUrl(path,60*60*24*30);if(s.error){setNotice(s.error.message);setUploading(false);return}await send('',{attachment_url:s.data.signedUrl,attachment_name:file.name,attachment_type:file.type});setUploading(false)}
 async function edit(m){setMenu(null);setEditing(m.id);setEditText(m.body||'')}async function saveEdit(m){const v=editText.trim();if(!v)return;const{error}=await supabase.from('direct_messages').update({body:v}).eq('id',m.id).eq('sender_id',uid);if(error)setNotice(error.message);else{setEditing(null);setEditText('')}}async function del(m){setMenu(null);const{error}=await supabase.from('direct_messages').delete().eq('id',m.id).eq('sender_id',uid);if(error)setNotice(error.message)}async function saveNick(){await supabase.from('friendships').update({nickname:nickname.trim()||null}).eq('id',selected.friendshipId);load()}
 if(loading)return <div className="sv-page sv-container"><div className="sv-card">Loading your study circle…</div></div>;return <div className="sv-page sv-container sv-friends-page"><div className="sv-page-header"><div><span className="sv-eyebrow">STUDY TOGETHER</span><h1>Friends</h1><p className="sv-page-subtitle">Build your study circle and chat while you learn.</p></div></div>{notice&&<button className="sv-social-notice" onClick={()=>setNotice('')}>{notice} ×</button>}<section className="sv-social-search sv-card"><span className="sv-section-label">FIND STUDY PARTNERS</span><h2>Search by name or username</h2><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search by name or username"/>{results.map(p=><div className="sv-person-row" key={p.id}>{av(p)}<div className="sv-person-info"><strong>{nm(p)}</strong><small>@{p.name}</small></div><button disabled={accepted.has(p.id)} onClick={()=>request(p.id)}>{accepted.has(p.id)?'Friends':'Add friend'}</button></div>)}</section>{requests.length>0&&<section className="sv-card"><span className="sv-section-label">PENDING</span><h2>Friend requests</h2>{requests.map(r=><div className="sv-person-row" key={r.id}>{av(r.user)}<div className="sv-person-info"><strong>{nm(r.user)}</strong><small>@{r.user?.name}</small></div><button onClick={()=>respond(r.id,'accepted')}>Accept</button><button onClick={()=>respond(r.id,'declined')}>Decline</button></div>)}</section>}<section className="sv-friends-layout"><div className="sv-card"><span className="sv-section-label">YOUR STUDY CIRCLE</span><h2>{friends.length} friend{friends.length===1?'':'s'}</h2>{friends.map(r=>{const p=r.user_id===uid?r.friend:r.user;return <button className={`sv-person-row sv-friend-button ${selected?.id===p?.id?'selected':''}`} key={r.id} onClick={()=>open(r)}>{av(p)}<div className="sv-person-info"><strong>{r.nickname||nm(p)}</strong><small>@{p?.name}</small></div><span>Chat →</span></button>})}{!friends.length&&<div className="sv-empty-social">👥 Your study circle is empty</div>}</div>{selected&&<aside className="sv-chat-panel sv-card"><div className="sv-chat-header">{av(selected)}<div><strong>{selected.nickname||nm(selected)}</strong><small>@{selected.name}</small></div><button onClick={()=>setSelected(null)}>×</button></div><label className="sv-nickname">Nickname<input value={nickname} onChange={e=>setNickname(e.target.value)} onBlur={saveNick}/></label><div className="sv-messages">{messages.map(m=><div className={`sv-message ${m.sender_id===uid?'mine':''}`} key={m.id}><div className="sv-message-bubble"><small>{m.sender_id===uid?'You':nm(selected)}</small>{editing===m.id?<div className="sv-inline-edit"><input value={editText} onChange={e=>setEditText(e.target.value)}/><button onClick={()=>saveEdit(m)}>Save</button></div>:<>{m.body}{m.attachment_url&&<a className="sv-chat-attachment" href={m.attachment_url} target="_blank" rel="noreferrer">📎 {m.attachment_name||'Attachment'}</a>}{m.sender_id===uid&&<button className="sv-message-menu-button" onClick={()=>setMenu(menu===m.id?null:m.id)}>⋯</button>}{menu===m.id&&m.sender_id===uid&&<div className="sv-message-menu"><button onClick={()=>edit(m)}>Edit</button><button onClick={()=>del(m)}>Delete</button></div>}</>}</div></div>)}</div><div className="sv-chat-tools"><button title="Camera" onClick={()=>cameraRef.current?.click()}>📷</button><button title="Attach file" onClick={()=>fileRef.current?.click()}>📎</button><input ref={cameraRef} type="file" accept="image/*" capture="environment" hidden onChange={e=>upload(e.target.files?.[0])}/><input ref={fileRef} type="file" hidden onChange={e=>upload(e.target.files?.[0])}/><input value={text} onChange={e=>setText(e.target.value)} placeholder="Message..." onKeyDown={e=>e.key==='Enter'&&send()}/><button disabled={uploading||!text.trim()} onClick={()=>send()}>{uploading?'Uploading…':'Send'}</button></div></aside>}</section></div>}
